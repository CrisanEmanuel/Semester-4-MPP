package agentie.networking.rpcProtocol;

public enum RequestType {
    CAUTA_EXCURSII,
    CAUTA_TOATE_EXCURSIILE,
    ADAUGA_REZERVARE,
    UPDATE_NUMAR_LOCURI_EXCURSIE,
    LOGIN,
    LOGOUT;
}
